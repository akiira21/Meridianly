# Backend API base URL (no trailing slash, no /api/v1 suffix)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional: Your production domain for SEO (sitemap, robots, OG images)
# Leave empty during development / testing
NEXT_PUBLIC_APP_URL=
